import React from 'react'

function About(){
    return(
        <React.Fragment>
           <h1>About</h1>
           <p> Line 2:  'propTypes'  is defined but never     used  no-unused-vars
            Search for the keywords to learn more about each warning.
            To ignore, add // eslint-disable-next-lineto the line before.</p> 
        </React.Fragment>
    )
}

export default About;